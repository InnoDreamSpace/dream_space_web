export type CreateShopDataType = {
  name: string;
  description: string;
  address?: string;
  logo?: string;
  contact: string;
};

export type ShopData = {
  id: string;
  name: string;
  description: string;
  address?: string;
  logo?: string;
  contact: string;
};
